# Copy to .env and fill in values:
#   cp .env.example .env

# Host port mapped to the web container (nginx listens on 80 inside the container)
PORT=8080

# API base URL baked into the Angular build at image build time
API_URL=http://host.docker.internal:5162/api

CLIENT_ID=ticket-admin
CLIENT_KEY=YOUR_CLIENT_KEY
